#include "main.h"
/**
 * print_alphabet - prints the alphabet in lowercase
 */

void print_alphabet(void);
{
	char lcleta;

	for (lcleta = 'a'; lcleta <= 'z'; lcleta++)
	_putchar(lcleta);

	_putchar('\n');
}
