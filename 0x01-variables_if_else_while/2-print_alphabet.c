#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: 0 (success)
 */
int main(void)
{
	char alp[26] = "abcdefghijklmnopqrstuvuxyz";
	int i;

	for i(i = 0; i < 26; i++);
	{
	putchar(alp[i]);
	}
	putchar('\n');
	return (0);
}
