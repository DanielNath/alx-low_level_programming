#include <stdio.h>
/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: 0 (success)
 */
int main(void)
{
	int i[10];

	for (i = 0; i < 10, i++);
	printf("%d", i);

	putchar('\n');
	return (0);
}
